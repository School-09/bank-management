# bank-management
Bank Management System - C++ OOP

```bash
$ cd src

$ g++ -std=c++23 main.cpp application/*.cpp application/controllers/*.cpp domain/entities/*.cpp infrastructure/repositories/*.cpp usecase/auth/*.cpp usecase/account/*.cpp -o debug/app

$ ./debug/app
```

*TODO*
- dùng expected
- nâng cấp lên c++23
- dùng namespace
- dùng regular expression
- lỗi session nhiều hơn 1 file